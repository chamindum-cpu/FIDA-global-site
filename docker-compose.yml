version: '3.8'

services:
  fida-web:
    image: chamindujanmor/fida-web:latest
    container_name: fida-web
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
